# This code is autogenerated.
# Code is generated by running custom script: python3 readme.py
# Any manual changes to this file may cause incorrect behavior.
# Any manual changes will be overwritten if the code is regenerated.

name: cli-scripts-deploy-moe-binary-payloads
on:
  workflow_dispatch:
  schedule:
    - cron: "24 4/12 * * *"
  pull_request:
    branches:
      - main
    paths:
      - cli/deploy-moe-binary-payloads.sh
      - infra/bootstrapping/**
      - .github/workflows/cli-scripts-deploy-moe-binary-payloads.yml
      - cli/setup.sh
permissions:
  id-token: write
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: check out repo
      uses: actions/checkout@v2
    - name: check OIDC configuration
      id: check-oidc
      run: |
          if [ -n "${OIDC_AZURE_CLIENT_ID}" ] && [ -n "${OIDC_AZURE_TENANT_ID}" ] && [ -n "${OIDC_AZURE_SUBSCRIPTION_ID}" ]; then
            echo "enabled=true" >> "${GITHUB_OUTPUT}"
          else
            echo "enabled=false" >> "${GITHUB_OUTPUT}"
          fi
      env:
        OIDC_AZURE_CLIENT_ID: ${{ secrets.OIDC_AZURE_CLIENT_ID }}
        OIDC_AZURE_TENANT_ID: ${{ secrets.OIDC_AZURE_TENANT_ID }}
        OIDC_AZURE_SUBSCRIPTION_ID: ${{ secrets.OIDC_AZURE_SUBSCRIPTION_ID }}
    - name: azure login
      if: steps.check-oidc.outputs.enabled == 'true'
      uses: azure/login@v1
      with:
        client-id: ${{ secrets.OIDC_AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.OIDC_AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.OIDC_AZURE_SUBSCRIPTION_ID }}
    - name: bootstrap resources
      if: steps.check-oidc.outputs.enabled == 'true'
      run: |
          bash bootstrap.sh
      working-directory: infra/bootstrapping
      continue-on-error: false
    - name: setup-cli
      if: steps.check-oidc.outputs.enabled == 'true'
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash setup.sh
      working-directory: cli
      continue-on-error: true
    - name: Eagerly cache access tokens for required scopes
      if: steps.check-oidc.outputs.enabled == 'true'
      run: |
          # Workaround for azure-cli's lack of support for ID token refresh
          # Taken from: https://github.com/Azure/login/issues/372#issuecomment-2056289617

          # Management
          az account get-access-token --scope https://management.azure.com/.default --output none
          # ML
          az account get-access-token --scope https://ml.azure.com/.default --output none
    - name: validate readme
      if: steps.check-oidc.outputs.enabled == 'true'
      run: |
          python check-readme.py "${{ github.workspace }}/cli/"
      working-directory: infra/bootstrapping
      continue-on-error: false
    - name: test script script
      if: steps.check-oidc.outputs.enabled == 'true'
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          set -e; bash -x deploy-moe-binary-payloads.sh
      working-directory: cli
